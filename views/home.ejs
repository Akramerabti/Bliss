<%- include('partials/header'); -%>
   
    <h2>Home</h2>
    <div class="relative">
        <!-- Button -->
        <button class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-700 transition duration-300 ease-in-out">
          Hover Me
        </button>
      
        <!-- Container (Initially Hidden) -->
        <div class="absolute hidden bg-gray-200 p-4 rounded shadow-md top-0 left-0 mt-8">
          <!-- Content -->
          <p>This is a container that appears when you hover over the button.</p>
        </div>
      </div>
    <% if (user) { %>
    <h2>hello
           <%= user.name %>
        <% } else{} %>
    </h2>
    
    <a href="/rooms" >Collaboardate</a>
    <%- include('partials/footer'); -%>